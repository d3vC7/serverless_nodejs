service: bannerService
org: micasadeelei

provider:
  name: aws
  runtime: nodejs20.x

resources:

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
